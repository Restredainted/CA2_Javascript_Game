import Component from "./component"

class Health extends Component {

    constructor(maxHP) {
        this.maxHP = maxHP;
        this.HP = maxHP;
    }

    damage(damage) {

    }

    heal(heal) {
        
    }

}



export default Health;
